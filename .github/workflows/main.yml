name: creating workflow
on:
  # push:
  pull_request: 
     branches:
          - main
          - feature
jobs:
  build:
       runs-on: ubuntu-latest
       steps:
          - run: echo "hello"

      
